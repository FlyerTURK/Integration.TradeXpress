# TradeXpress / Integration — Çalışma Kuralları (TEK KAYNAK)

> **Kullanıcı = HAKAN** (hitap: Hakan). Makine/git hesabı ("Umut"/umutt) ve ts.net adı OĞLUNUN adınadır — kullanıcı adı sanıp "Umut" diye hitap ETME (2026-07-24 düzeltmesi).

> Bu dosya projenin **tek always-loaded kural kaynağıdır**. Yeni kural çıkınca AYRI memory dosyası açma — buraya bölüm/madde ekle. Dosyaya-özel detay kurallar `.claude/rules/` altında (`paths:` ile koşullu yüklenir). Pazarlıksız kuralların bir kısmı ayrıca `settings.json` deny + PreToolUse hook ile **mekanik** bloklu.

## 0) Karar yetkisi
- **Teknik/mimari karar bende.** Kullanıcı ihtiyacı/iş hedefini söyler; ben değerlendirir, kararı verir, gerekçesini açıklarım. "Hangisini istersin" diye SORMA. Kullanıcı isterse itiraz eder.
- `AskUserQuestion` yalnız **iş/ürün** kararı için (kapsam, öncelik, ürün hedefi). Teknik seçim için değil.
- Kullanıcının geçmiş teknoloji tercihleri bağlayıcı değil; en uygun çözümü öner, sapıyorsan nedenini söyle.

### 0.1) Ön-uçuş kontrol listesi (2026-07-25)
> **"Meclis" (5 lens + kör subagent oturumu) AYNI GÜN KALDIRILDI.** Hakan'ın gerekçesi: *"aşırı zaman alıyordu. seninle zaten ultracode ile çalışıyoruz ve senin alt agent çalıştırma düzenini bozma ihtimalinden çekindim."* Yani asıl sorun isabetsizlik değil **ÇİFT ORKESTRASYON**: ultracode zaten göreve göre subagent dallandırıyor; meclis onun üstüne SABİT 5 lensli rakip bir katman koyuyordu (3 oturum · 22 dk · ~1,1M token). Lens töreni · kademe beyanı · kör meclis · K0/K1/K2 kademeleri: **hepsi kalktı.** Fan-out artık göreve göre şekillenir, sabit şablona göre değil. Tarihçe: `.claude/research/governance/MECLIS-LOG.md`.

**Kalan TEK şey — ÖN-UÇUŞ. Her kod işinde, maliyet ~0, ATLANAMAZ.** Kod yazmadan önce 4 kutu:
`[varsaydım mı / doğruladım mı?]` · `[bu bileşen-servis zaten var mı — grep'ledim mi?]` · `[hangi test kırılabilir?]` · `[geri alınabilir mi?]`

**SESSİZ uygulanır** — beyan yok, tören yok, ek çıktı yok. Yalnız bir kutu KIRMIZI çıkarsa tek satırla söylenir.

*Neden bu kaldı: 2026-07-25'in belgeli hatalarını yakalayan şey meclis DEĞİL buydu — N11 "1"≠"01" · elde yeniden yazılan `BranchEditFields` · kırılan `OrgCodeUniquenessTests` · zaten var olan `ShowValidationToasts`. Kutu #2'yi UI'da sorup DOMAIN servislerinde sormamak da `GeographyResolver`'ı üçüncü kez yazmama yol açtı → kutu #2 **her katman için** sorulur.*

Geri-dönüşsüz · mimari · kural değiştiren işlerde ayrı mekanizma YOK: **§1 aynen geçerli** (DUR + plan + BEKLE + onay).

## 1) Onay gereken işler (önce öner, BEKLE)
- **Denge:** Yapıcı/eklemeli/**geri-alınabilir** normal iş = SORMA, yap (build, kolon/property/alan/dosya ekleme, normal edit, sıralama/stil ekleme). **SORMA gereken = CİDDİ/YIKICI:** çalışan bir şeyi silmek/kökten yeniden yazmak/değiştirmek, geri-dönüşsüz işlem, büyük yapısal dönüşüm. Ölçüt: *geri alması zor mu / mevcut emeği yok ediyor mu?* Evet → DUR + plan (ne·neden·hangi dosya·geri-alınabilir mi) sun + BEKLE.
- **Commit:** onaysız commit YOK. AMA "commit'leyelim mi?" diye SORMA/hatırlatma yapma — kullanıcı farkında, sürekli sormak rahatsız ediyor. Yalnız "commit" deyince yap.
- **Override:** mevcut çalışan stil/ikon/default/davranışı **başka bir şeyle değiştireceksen ÖNCE SOR.** Yeni ekleme serbest; mevcut olanı ezmek onay ister.
- **Çok-dosyalı tekrar:** aynı düzeni 2+ dosyaya uygulayacaksan önce 1 örnek göster/plan sun, onay al, SONRA yaygınlaştır. Topyekûn sweep YOK.
- **CSS/stil:** yeni `.css`/`.razor.css`/`<style>`/sınıf — inline'ı "temizlik" diye CSS'e taşıma dahil — önce dosya+sınıf+seçici+neden belirt, onay bekle.
- **Teşhis önce:** "şöyle olmalı" demeden kök nedeni DOĞRULA (varsayım ≠ kanıt). Referans varsa önce oradaki çalışan deseni incele.

## 2) ASLA onaysız (yıkıcı kısayol YASAK)
Tıkanınca kolay yola sapıp mevcut işi silme/kökten değiştirme YOK. Refleks: DUR → kök neden + 1-2 küçük geri-alınabilir seçenek → geri-dönüşsüzleri işaretle → onay bekle.
- **Mekanik bloklu** (deny + `guard.ps1` hook — tam liste orada): git reset --hard / checkout -- / force-push / --amend / branch -D / clean -f · `ef database drop` / `migrations remove` · rm -rf & toplu silme · anonim-erişim attribute edit'i · Migrations/snapshot elle edit. Aynı sınıf (hook'suz): `WHERE`'siz UPDATE/DELETE, truncate, şema sıfırlama, seed ezme, commit'siz değişiklik atma.
- **Kod:** kökten yeniden yazım, çalışan implementasyonu silme, tıkandın diye yaklaşımı terk etme.
- **Mimari:** katman kaydırma (UI↔API), merkezi yolu (CrudLayout/StateService/Framework modülü) bypass edip paralel tek-kullanımlık yapı.
- **Güvenlik:** auth/`[Authorize]` kapatma, multi-tenant `IDataFilter` disable, validation/concurrency/guard kaldırma "çalışsın diye".
- **Sahte baypas:** mock/fake dönme, `catch {}` ile kök neden gizleme, `#pragma warning disable`/`<NoWarn>`, test/assertion zayıflatma.
- **Diğer:** sabit paket sürümü (DevExpress 25.2.8/ABP) hatadan kaçmak için oynama; sessiz kapsam düşürme; hedefli düzeltme yeterken toplu find-replace/reformat; Program.cs/DI/DbContext/modül global config'i yerel semptom için değiştirme; kullanıcının çalışan host'unu habersiz öldürme.

## 3) Build / host akışı
- Host'u **terminal sahiplenir** (VS değil, F5 yok). Host'u ben `dotnet run -c Debug` ile başlatırım.
- **Host başlatma PENCERE AÇMAZ** (2026-08-07 Hakan talimatı): görünür cmd penceresi YOK — arka planda/gizli başlat (`Start-Process dotnet ... -WindowStyle Hidden`), ayakta olduğunu port dinlemesiyle DOĞRULA ve raporla. (Önceki "process başlamadı" karışıklığının sebebi pencere değil, doğrulamasız rapordu.)
- Değişiklikte **sadece `dotnet build` + doğrula + kısa rapor**. **Otomatik kill+restart ETME** — kullanıcının odağı dağılıyor. Restart'ı yalnız "yenile/restart/ayağa kaldır" deyince ya da bir grup değişiklik bitince yap.
- **Aradaki adımlarda derleme bile yapma** — değişiklikleri biriktir, grup sonunda derle.
- **Host çalışırken build = stale DLL** ([dll-kilit]): önce host'u ÖLDÜR (`Get-NetTCPConnection -LocalPort 44318 -State Listen | Stop-Process`), port boş doğrula, SONRA build, sonra `dotnet run --no-build`.
- Server-side değişiklik Blazor host'u (44318) restart ister, HttpApi.Host (44388) değil (in-process).

## 4) Kod stili & isimlendirme
- **Identifier'lar İNGİLİZCE** (class/property/method/enum/const/namespace/dosya). **Yorumlar Türkçe.** ERP terimi İngilizce'ye çevrilir: Bilanço→BalanceSheet, Cinsi→Category, Bakiye→Amount/Balance, Takoz→Bullion, Pırlanta→Jewelry (bizde Mücevher), Taş→Stone, İşçilik→Labor, Çeşni→Alloy, Şube→Branch, Kasa→Vault, Hesap→Account. Emin değilsen sor.
- **⭐ TEKNİK TERİMİ TÜRKÇEYE ÇEVİRME — koddaki adı yaz** (Hakan, 2026-08-21: *"Tohum, çekirdek, tahta gibi Türkçeye çevirdiğin tanımlamaların var ve bu teknik tanımlamaları Türkçe yapınca bazen anlaşılamayabiliyor."*). Cümle Türkçe kalır, TERİM İngilizce: "tohum" değil `seed` · "tahta" değil `board` · "çekirdek" değil `core` · "kapı" değil `guard` · "ayna" değil `projection` · "delil defteri" değil `PushHistory` · "uç" değil `endpoint` · "fotoğraf" değil `snapshot` · "bulgu" değil `issue`. İlk geçişte bir kez `seed` (tohum) diye açıklanabilir; tekrarında sade terim kalır.
  **Gerekçe iki katmanlı:** ① çevrilen kelime grep'te koddaki adla EŞLEŞMEZ — "delil defteri" arayan `PushHistory`'ye ulaşamaz; ② bir Türkçe kelime çoğu zaman birden çok İngilizce kavramı yutar ("damga" = reçete `Stamp` + `LastSyncedAt` + `CompanyId` ataması; "çekirdek" = `Core` öneki + taban varyant + kanal `Override`'ının karşıtı olan ERP tarafı) ve hangisi kastedildiği ancak dosya açılınca anlaşılır.
  **Kod karşılığı OLMAYAN eğretileme KULLANMA** (sarmal · kanca · çivilemek · mekanik ağ · yüzey · iskelet · emniyet supabı): kastettiğin sınıf/metot/test adını yaz — "konvansiyon testi `MediaContextPairingTests` şunu sabitler" gibi.
  **KAPSAM yalnız yorumlar değil:** rapor/sohbet metinleri ve commit mesajları da dahil. *(Kanıt: kendi commit mesajımdaki "tahta kayıt hatası" ifadesi ertesi gün BENİM tarafımdan da çözülemedi.)*
  **İSTİSNA — kullanıcının İŞ DİLİ çevrilmez ve İngilizceleştirilmez** (bu bir KORUMA listesidir, tersi yönde de geçerlidir): milyem · has · ayar · işçilik · çeşni · maden · takoz · hurda · vadeli · mamül · mücevher · taş · emtia · reçete · varyant · fiş · cari · kasa · şube · bakiye · bilanço · kur · ürün · sipariş · kargo · muadil. Bunlar ekranda da bu adlarla görünür; identifier'ları İngilizce olsa bile (§4 ilk madde) YORUMDA Türkçe kalırlar.
  *Envanter (2026-08-21): 20 terim, ~2400 geçiş. Geriye dönük temizlik INCREMENTAL — dokunduğun dosyada düzelt, ayrı "terim refactor" görevi açma.*
- **⭐ KAVRAM ADI UYDURMA — ve kendi çerçeveni HAKAN'A MAL ETME** (2026-08-21 düzeltmesi; Hakan: *"böyle bir şeyi ben senden asla istemedim. Ben de sana şunu söyledim. Hatalar neredeyse derinlemesine üründen aşağıya uyarıları Kırmızı renkte belirt demiştim."*). Yaşanan: satışa hazırlık paneline "ürün kokpiti" adını BEN taktım ve sonra kavramı onun isteğiymiş gibi anlattım; onun istediği şey **bulguların üründen aşağıya, en yüksek önem derecesine göre KIRMIZI yayılması**ydı.
  **İki kural:** ① kullanıcının İŞLEVSEL isteğini kendi icat ettiğin bir kavram adıyla anlatma — istenen davranışı kendi adıyla yaz ("satışa hazırlık paneli" = `ProductSaleReadinessPanel`, "bulgu yayılımı" = `SaleReadinessIssueIndex`); ② `(Hakan, <tarih>: "…")` atfı YALNIZ onun GERÇEK sözü içindir. Parafraz ya da senin çerçevense öyle işaretle ("benim önerim", "ölçümden çıkan").
  **Gerekçe:** bu dosya projenin hafızası. Uydurma bir atıf, altı ay sonra "kullanıcı böyle istemişti" diye savunulan yanlış bir tasarım üretir ve kimse geri dönüp sorgulamaz.
- **Pazaryeri "Listing" kavramı YASAK** (2026-07-07 kullanıcı kararı): kanal-ürün entity'si `SalesChannelTr{Pazaryeri}Product` deseninde adlandırılır (ör. `SalesChannelTrN11Product`, `SalesChannelTrTrendyolProduct`) — kanal ailesi `SalesChannelTr{Pazaryeri}` ile hizalı. Push metotları `PushTo{Pazaryeri}Async`.
- **Hiçbir tipi inline tam-nitelikli yazma** (her tip, sadece Guid değil): `System.Guid`→`Guid`. Namespace ön-eki = koku. C# namespace'leri **GlobalUsings.cs**'te, Razor `@using`'leri **_Imports.razor**'da topla; kodda kısa ad. Redundant/duplike using'i o dosyaya dokununca sil — ama INCREMENTAL, ayrı "using refactor" görevi açma.
- **Mühendislik prensiplerini aktif uygula** (DRY/SOLID + KISS/YAGNI/Composition-over-Inheritance/Connascence/Fail-fast/SSOT/Tell-Don't-Ask/Encapsulation/Least-Astonishment...). Dokunduğun kapsamda ihlal görürsen **sadece raporlama — düzelt.** En üst bilinç: **kod ne kadar merkezi/yeniden-kullanılabilir olursa diğer UI/yeni projelerde devralınması o kadar kolay** → en merkezi, override ile genişleyen yerleşimi seç. Generic/reusable kod **Framework**'e (TradeXpress'e değil).

## 5) Dosya / klasör düzeni (çöplük yapma)
- **Feature-klasör = namespace.** Namespace klasör yolunu birebir izler, TÜM katmanlarda + testlerde aynı (ör. `Financials/{CurrencyUnits,Parities,ExchangeRates}`). Yeni dosya doğru bucket'a, kök/gevşek dizine bırakma.
- **Taşıma `git mv`** (geçmiş korunur). **EF Migrations/snapshot'a DOKUNMA** — namespace değişse de snapshot bayatlar ama runtime'ı etkilemez, sonraki gerçek migration'da yenilenir.
- **Plan-modu disiplini:** DB migration · çekirdek iş mantığı · prod/güvenlik config → uygulamadan ÖNCE plan sun.
- **Git güvenlik ağı:** her oturum öncesi temiz commit; bozulursa geri al.

## 6) Mimari sabitler (stack)
- **ABP.IO** (ücretsiz) + **DevExpress.Blazor 25.2.8** (lokal NuGet, TAM sabitleme — sürüm oynama) + **EF Core 10**. `Integration.Framework` = ayrı Core değil, her projeye `DependsOn` ile eklenen **ABP modülü**.
- **Blazor SERVER** (WASM değil): `ILogger` server-log'a gider; `IWebAssemblyHostEnvironment` YOK; client modülü server'ın `DependsOn`'unda değil → **servis/mapper'ı server modülde de elle kaydet**; `BusinessException` in-process lokalize olmaz.
- **Portlar:** Blazor host `:44318` · HttpApi.Host `:44388`. Kanonik URL `https://umut.taile7a850.ts.net:44318` (WASM authority tek-değerli → localhost değil ts.net). Cert: `E:\Kodlarim\Yeni\certs\`.
- **Org hiyerarşisi:** Şirket→Şube→Kasa (OrgTreeManager: otomatik kurulum + en-az-1-child + HQ-devir-önce-sil). Yetki: rol/izin tenant/company/branch/vault **scoped** (cascade + dar override; working context = company+branch).
- **TÜM emtia katalogları PER-COMPANY** (2026-07-25, görev #4): Metal·Stone·Jewelry·Good·Scrap·Future·Service **`ICompanyOwned`** — `CompanyId` ZORUNLU (`Guid`, `Guid?` değil), benzersizlik `(TenantId, CompanyId, Code)` + soft-delete farkındalı filtre. Sahiplik client'tan DEĞİL working company'den damgalanır (`CompanyOwnershipGuard.ResolveOwnerCompanyId` → şirket yoksa fail-closed). Seeder'lar tenant'ın HER şirketine ayrı set kurar (org seed'inden SONRA). **Eski "emtialar host-seviyesi (TenantId=null)" inancı YANLIŞTI** (canlıda 0 host satırı) ve sahipsiz "holding" katmanı cross-company manipülasyonun taşıyıcısıydı — kaldırıldı. Mekanik ağ: `CompanyScopedFilterTests.Ownerless_commodity_cannot_be_constructed` (7 aile). *(Cash + CurrencyUnit + Country hâlâ host‖tenant kataloğu — emtia değil.)*
- **EMTİA AİLELERİ VARYANT AÇISINDAN ÜÇ KATEGORİDİR** (2026-08-08 ölçümü + Hakan teyidi). Bu ayrım hiçbir yerde yazılı değildi ve aynı gün **üç kez** koddan yeniden çıkarıldı; her turda yanlış gruplama riski doğdu (bir kez "Scrap/Future seeder'ı da ana varyant açsın" diye YANLIŞ öneri üretildi, Hakan düzeltti).
  **① Tam varyantlı — `Product` · `Metal` · `Good`:** hem `EntityVariantsPanel` hem `*VariantDetail` uzantısı var. Aileye özel alanlar VARYANT seviyesinde yaşar (ör. `MetalVariantDetail.EntryLabor` — her varyantın kendi işçiliği).
  **② Uzantısız varyantlı — `Jewelry`:** panel VAR, `*VariantDetail` uzantısı YOK. Varyant kod/ad/barkod/stok taşır ama aileye özel alan taşıyamaz → **tüm varyantlar entity seviyesindeki fiyatı PAYLAŞIR** (`Jewelry.EntryPrice`/`ExitPrice`). 22 ayar ve 14 ayar bilezik ayrı varyant olabilir ama ayrı FİYATLANAMAZ. **Jewelry bu konuda KAPSAM DIŞI** (2026-08-08 Hakan kararı): `JewelryVariantDetail` açılmayacak, kısıt bilinerek bırakıldı. *(Düzeltme 2026-08-15: bu madde eskiden Stone'u da sayıyordu — BAYATTI. Stone 2026-08-09'dan beri VARYANTSIZDIR: "her taşın parmak izi ayrıdır"; `StoneAppService.SaveGraphAsync` varyant kolunu atlar, `StoneLayout` varyant sekmesi kaldırıldı, yeni taş kaydında ana varyant bile açılmaz. `IStoneAppService.GetVariantPickerListAsync`/`GenerateVariantsAsync` ölü yüzeydir.)*
  **③ Varyantsız — `Future` · `Scrap` · `Service` · `Stone`:** panel YOK, uzantı YOK, seeder varyant ÜRETMEZ, app service varyant senkronu ÇAĞIRMAZ. **"Vadeli varyant barındırmaz"** (Hakan, 2026-08-08). Hizmet stoklanan emtia değil, reçeteye giren ÜCRET kalemidir (`RecipeComponentType.Service`; katalog kaydı bile açılmaz) — stoklanmayanın varyantı olmaz.
  **VARYANTLI HER EMTİA REÇETEDE MADEN GİBİ DAVRANIR** (2026-08-15 Hakan kuralı): reçete panelinin combo'su ① ve ② için VARYANT listesi gösterir (`Metal` → `MetalVariantLookupDto`, `Good`/`Jewelry` → `CommodityVariantLookupDto`; `GetVariantLookupAsync` yassı emtia×varyant), satıra `CommodityVariantId` yazılır. Good'da fiyat VARYANTTADIR ve `RecipeCostPopulator` seçili varyantın fiyatını okur (UI bu alanı yazmadığından hep ana varyanta düşüyor, satır yanlış fiyatlanabiliyordu — kapatıldı); Jewelry'de seçim kimlik/stok içindir, fiyat paylaşılır. Grid toplamı da seçili varyanttan (UI ile sunucu aynı sayıyı göstersin). Legacy satır (`CommodityVariantId=null`) ana-varyant fallback'iyle çalışmaya devam eder. Mekanik ağ: `GoodVariantLookupTests`.
  **Sonuçları:** "ana varyant pasifleştirilemez" ve "tek varyant ise kod = emtia kodu" kuralları yalnız ① ve ② için anlamlıdır, ③'te KONUSUZDUR. `MetalSeeder.RepairMainVariantIdentityAsync` Metal'e ÖZGÜdür ve diğer ailelere YAYILMAZ (②'de canlı kayıt yok, ③'te varyant yok). Canlı ölçüm 2026-08-08: Metal 86 kayıt/86 varyant · Scrap 20/0 · Future 18/0 · Good/Jewelry/Stone/Service 0 kayıt. **Scrap, Future ve Stone'un varyantsız olması EKSİKLİK DEĞİL, tasarımdır** — seeder'a/servise varyant üretimi EKLEME.
- **KARGO YALNIZ SATIŞ KANALI SEVİYESİNDE** (2026-07-26 Hakan kararı — çekirdek katman SİLİNDİ): çekirdek `ShipmentTemplate` + `TrCarrier` entity/servis/UI/tablolarının TAMAMI kaldırıldı; ürün üzerindeki kargo şablonu bağı (`Product.ShipmentTemplateId`/`ShipmentTemplateName`) ve N11 K1 köprüsü (`N11ShipmentTemplate.ShipmentTemplateId`) de sökülüldü. **Gerekçe:** kargo şablonu ürünün değil KANALIN özelliğidir (aynı ürün her pazaryerinde farklı şablonla gider) ve her pazaryerinin kendi kuralları olduğundan ortak çekirdek katman gereksiz soyutlamaydı. Kalan yapı: `N11ShipmentTemplate` (kanal şablonu) + `N11ShipmentCompany` (host-global N11 firma aynası). **Sistem CARİ AÇMAZ:** kısa ömürlü `KARGO` hesabı + 68 alt hesap otomasyonu da geri alındı — şirket kendi cari planını kendisi yönetir; kargo firmasının carisi kullanıcı tarafından bağlanır. **KANAL BAŞINA VARSAYILAN FİRMA KURULDU** (2026-08-10 Hakan: *"satış kanalı edit formda default kargo firması seçili olsun"*): `SalesChannelTrTrendyol.DefaultCargoProviderId` + kanal edit formunda ön-dolu combo. **Varsayılanı SUNUCU seçer** (`TrendyolDefaultCargoProviderResolver`; tercih Trendyol Express `17`, yoksa en küçük sayısal id) — üç yol (form · sihirbaz · doğrudan API) aynı cevabı görsün diye; istemcide "listenin ilkini seç" deseydik API'den açılan kanal başka firmaya düşer ve fark ancak ilk gönderimde görülürdü. Yalnız **create**'te ve yalnız alan BOŞSA damgalanır — update'te varsayılana geri dönülmez (bilinçli temizleme geri alınmaz). Sihirbazın kargo adımı önceden **tamamen görseldi** (seçim yerel alanda kalıp kayboluyordu); artık dar bir uçla (`SetDefaultCargoProviderAsync`) kanala yazılıyor — tam `UpdateAsync` kullanılamaz çünkü sihirbaz mevcut-kanal kipinde kimlik alanlarını yüklemiyor ve tam DTO onları sessizce boşaltırdı. Liste `TrendyolCargoProviderSeeder` ile **host-global** kurulur ve **Trendyol hesabından BAĞIMSIZDIR** (canlıda 10 firma; Trendyol'un firma döndüren HTTP ucu YOKTUR → resmî statik tablodan seed). **Kalan hedef:** varsayılan cari alt hesap ilk kullanımda (şablon içe aktarımında, firma öksüzse) kullanıcıya SORULUR; aynı firma farklı kanallarda aynı cariyi gösterebildiğinden borç tek bakiyede birikir. Reçetedeki kargo kalemi ORTALAMA maliyettir (tek paket varsayımı — fiyatlama için); gerçek maliyet sipariş sürecinde kullanıcı tarafından girilir.
- **Yerel ≠ Bilanço birimi** — kur görüntüsü YERELE, pozisyon/değerleme BİLANÇOYA re-base; karıştırma. Detay + yön kuralları: `.claude/rules/financials.md`.
- **KURU OLMAYAN BİRİM DEĞERLEMEYE GİRMEZ** (2026-08-05): motor, hiçbir kur bağlantısı olmayan birime `1/1` **yer tutucu** verir ve `EffPrice.RateMissing` ile İŞARETLER. Bu yer tutucu kur DEĞİLDİR: `GetValuation*` onu **eler** (sözlükte yokluk = "kur yok"un tek temsili; aşağı akış zaten böyle okuyor — `PositionReportAppService` `MissingRate = val == null`, `ProductRecipeCostCalculator` satırı net toplama almaz), gösterim yolu sayı yerine "Kur yok" yazar, yerel para birimi kursuzsa re-base UYDURULMAZ (pivot görüntüye düşülür). **Neden:** bayrak yokken HAS kursuz olduğu halde panoda "1" görünüyordu ve 7 gram has altın reçetede "7,00 TRY" olarak fiyatlanıyordu; dahası `MissingRate` fail-fast ağı baypas oluyordu (`TryGetValue` uydurma değerle hep başarılı dönüyordu). Eksiklik takip zinciri boyunca TAŞINIR (kursuz HAS'ı takip eden madenler de işaretlenir). Mekanik ağ: `EffectivePriceAppServiceTests` (bayrak + eleme + yabancı şirket re-base'i).
- **PAZARYERİ AŞIRI SATIŞ SAVUNMASI** (2026-08-05 Hakan kararları): ① **0 kurulabilir varyant → satış durur** — muadil materializer hiç varyant üretmezse push aday listesi boşalır; bu durumda `NoSyncableSku` fırlatmak yerine bilinen TÜM SKU'lara **adet 0** gider (fiyat korunur; liste kapanmaz, "Out_Of_Stock" görünür). Stok gelince mevcut dirty-check gerçek adedi KENDİLİĞİNDEN geri yazar — ayrı "yeniden aç" yolu YOK. ② **N11 `bundle` DAİMA `false`** — alan gönderilmezse N11 `true` varsayıyor ve müşteri aynı ürünün iki varyantını tek sepete koyabiliyor; varyantlarımız aynı maden havuzunu paylaştığı için bu doğrudan aşırı satış kapısı. Kapsam **istisnasız tüm ürünler** (ben yalnız `StockPolicy=Calculated` ile sınırlamayı önerdim, kullanıcı "her zaman false" dedi). **⚠ İkisi de kısmi çözüm:** aynı sepeti/anı engeller, AYRI siparişlerdeki çakışmayı engellemez — kök çare rezervasyon (aşağıda).
- **PUSH EMNİYET ALANLARI: PAY + FİYAT BANDI** (2026-08-08): kanal-ürün başına iki opsiyonel alan, kural gövdesi TEK yerde (`ChannelPushGuard`; N11 + Trendyol ortak taban paylaşmadığı için aritmetiği iki entity'ye kopyalamak connascence üretirdi). ① **`SafetyStock`** = kanalda GÖSTERİLMEYEN adet; push satırının nihai stoğundan `max(0, stok − pay)` ile düşülür. Aşırı satış savunmasının DÖRDÜNCÜ katmanı: diğer üçü "aynı an/aynı sepet" çakışmasını kapatır, pay **senkron turları ARASINDAKİ pencereyi** daraltır. Uygulama noktası TEK: `BuildPushRowsAsync` çıkışı — üç çağıran (tam push · hafif senkron · önizleme) aynı paylı sayıyı görsün diye; çağıranlardan birinde uygulansaydı dirty-check her turda "değişti" der ve N11'e sonsuza kadar aynı ürün yazılırdı. Pay stoğun KAYNAĞINA bakmaz (`OverrideStock` dahil — ikinci bir semantik Least-Astonishment ihlali olurdu). ② **`MinPrice`/`MaxPrice`** = insansız repricing motorunun (15 dk) son emniyeti: türetilmiş fiyat bant dışına düşerse ürünün TÜM push'u DURUR. **Kırpma YOK** — kırpmak motorun ürettiği yanlış sayıyı meşru bir fiyata çevirip hatayı gizlerdi (kursuz birime uydurma kur yazmama kararıyla aynı felsefe). İhlal eden satırı atlayıp kalanı göndermek de yasak (sessiz kapsam düşürme). Adet-0 dalı guard'dan MUAF (`rows` zaten boş — satışı durdurma yolu bandın felç edeceği yer değil). `null` = "sınır konulmadı" (`0` meşru bir taban beyanıdır); negatif pay ve `min>max` kayıtta reddedilir. Trendyol alanları bu dilimde AÇILDI, tüketimi row-builder portunda. Mekanik ağ: `SalesChannelTrN11ProductSafetyStockTests` (5) · `SalesChannelTrN11ProductPriceGuardTests` (6) — ikisi de sabotajla doğrulandı. *(Güncelleme DTO'sunun alanları taşıması ayrıca pinli: taşınmasaydı her kayıt kullanıcının kurduğu emniyeti sessizce silerdi.)*
- **İÇE AKTARILAN ÜRÜN → REÇETE (sihirbaz adımı; 2026-08-05 Hakan kararları — 2026-08-06'da KURULDU):** mağaza içe aktarımı ürünü `StockPolicy=Fixed` + reçetesiz getiriyor → orkestrasyonun hesaplayacağı bir şey olmuyor (canlıda 103 içe aktarılmış ürünün TAMAMINDA emtia çözülemiyordu; yalnız elle kurulan 1 ürün Calculated'dı). Eksik olan sihirbaz adımı: **kullanıcıya "ne tür emtia?" SORULUR** — Metal/Scrap/Good → `CatalogCommodity` satırı + katalog kaydı SİHİRBAZ İÇİNDE oluşturulur; Service → `RecipeComponentType.Service` ücret satırı (katalog kaydı YOK). **Sınıflandırma MANUELDİR, yazılım tahmin ETMEZ** — ama stok kodu/ad/görsel/varyant içe aktarılan üründen ÖN-DOLDURULMUŞ gelir (bir ürün nadiren birden çok emtiadan oluşur → çoğunlukla tek satır). **OTORİTE DEVRİ:** sisteme bağlandıktan sonra pazaryerinde önceden duran stok ve fiyat GEÇERSİZDİR; ikisini de sistem belirler (birleştirme/koruma mantığı YOK). **İki yanı birden kapalıdır:** sınıflandırma anında mevcut kanal aynası temizlenir (`ChannelOverrideAuthority.TransferAuthorityAsync`) **VE** üç kanalın içe aktarımı da `Calculated` üründe `Override*` yazmayı BIRAKIR — yalnız temizlemek yetmezdi, ilk import devri geri alırdı (push zinciri override'ı önceler). Kurulan yapı: `ProductCommodityClassificationPanel` (kanal-agnostik ortak adım, **atlanabilir**; aday listesi kanaldan değil ŞİRKETTEN çıkar → eski importlar da yakalanır) + `ProductCommodityProvisioner` (TEK çağrı; emtia ailenin KENDİ app service'iyle açılır ki şirket damgası/kod benzersizliği korunsun) + `ProductRecipeLineWriter` (reçete yazımının tek yolu). **Atlanan/kararsız ürün `Draft` kalır ve satışa çıkmaz** — güvenlik zorunluluktan değil STATÜDEN gelir; ama sessiz değildir, sihirbaz özetinde sayılır. Yalnız hizmet satırı olan ürün `Unlimited` olur (`Calculated` yapmak stok zincirinin veri bulamayacağı bir hesap açar, sonuç sessizce 0'a düşerdi). Mekanik ağ: `ProductCommodityProvisionerTests`. *(Stok zincirinin Good'u görmemesi de 2026-08-06'da giderildi — bir alttaki madde.)*
- **STOK ZİNCİRİ EMTİA-AİLESİ FARKINDALIDIR** (2026-08-06): tetik → ters-endeks → okuyucu → satılabilir adet zincirinin tamamı `ProcessType.Metal`'e sabitlenmişti; mamül (`Good`) reçeteli ürün, mamül stoğu değişse bile HİÇ uyanmıyor, kanalda eski adet canlı kalıyordu. Kurulan yapı: `CommodityStockChangedEto`/`CommodityStockKeyEto` · `ICommodityStockReader` (aile başına rapor dalı) · `CommodityStockKey(Family, CommodityId, VariantId)` · `RecipeCommodityRequirement`. **Aile ANAHTARIN parçasıdır** — `CommodityId` FK'sız snapshot ve aynı Guid farklı ailede çakışabilir; aileyi anahtara koymak "3 gram"ı "3 adet" sanma ihtimalini sözleşme uyarısıyla değil **yapısal** olarak kapatır. **Kapsam listesi TEK yerdedir** (`CommodityStockFamilies.Tracked` — bugün Metal + Good). **KAPSAM BİLİNÇLİ KARARDIR, BORÇ DEĞİL** (2026-08-21 Hakan: "Böyle kalsın" — Metal+Good dışındaki ailelerden reçeteli ürün satışı PLANLANMIYOR; fail-closed 0 stok doğru davranış. Best-practice karnesindeki "Scrap zincir dışı" bulgusu bununla KAPANDI — Tracked'i genişletme önerisi yeniden açılmasın): tetiği yayan · ürünleri bulan · stoğu okuyan aynı listeye bakar, biri diğerinden dar kalırsa o aile sessizce zincir dışında kalır. **Boyut varsayılmaz:** okuyucu hem miktar hem adet döner, reçete ihtiyacı hangi boyutlarda beyan edildiyse hepsi kısıt sayılır (Maden bilinçle YALNIZ gram kısıtlar — `Amount = Quantity × StableQuantity` olduğundan adet ikinci kez daraltırdı). Desteklenmeyen aile **boş sözlük** → 0 stok (fail-closed); "sınırsız" saymak aşırı satış kapısıdır. Muadil-grup dalı Metal-only kalır (`SubstitutionGroupItem.MetalId` tanım gereği madene bağlı). Mekanik ağ: `SellableStockCalculatorTests` (aile yalıtımı + iki boyut) · `RecipeCommodityIndexAffectedTests` (çapraz eşleşme yok) · `GoodReportReservationTests`.
- **SİPARİŞ REZERVASYONU** (2026-08-05 Hakan kararları — 2026-08-06'da KURULDU): pazaryeri siparişi bugün stoğa HİÇ dokunmuyor (`OrderAppService`'te ne fiş ne stok var; `OrderLineActionStatus` yalnız N11 bildirim durumu) → sipariş ile fiş arası boyunca stok hem satılmış hem satılabilir görünüyor. **En geniş aşırı satış deliği budur.** Altyapı hazır: `ProcessPaymentType.Reservation` fiş satırı fiziksel `Net`'e girmez, `ReservedOut`'a yazar, `AvailableAmount = Net − ReservedOut` zinciri `SellableStockCalculator`/muadil çözücüsünü besliyor — yani rezervasyon bir FİŞ olduğu için `CommodityStockChangedEto` zinciri kendiliğinden çalışır, **yeni altyapı gerekmez**. Kurallar: sipariş ÇEKİLDİĞİ anda rezerve edilir (`OrderSyncBackgroundWorker` 2 dk) · rezerve edilen = eşleşen varyantın REÇETESİNDEKİ madenler (varyant kodu kombinasyonu taşır) · **süre aşımı YOK** ("sipariş siparıştir") · fiş **merkez şube/merkez kasa**da kesilir, hazırlayan kasa rezerveyi **state machine** ile fiziki çıkışa çevirir · fiyat farkı ASLA türetilmez, yalnız kullanıcı girer. **Kurulan yapı:** `OrderReservation` (sipariş başına tek; İKİ EKSEN — `OrderReservationStatus` stok · `OrderCancellationDecision` insan kararı; başlangıç `Blocked` = fail-closed) + `OrderFulfillmentLink` (kalem↔fiş satırı **ÇOKA-ÇOK**: 23gr+27gr → tek 50gr senaryosu; `PriceDifference` **null=beyan edilmedi ≠ 0="fark yok" beyanı**) + `OrderReservationVoucherMaterializer` (worker bağlamı — `[Authorize]`'lı `SaveLineAsync` KULLANILMAZ, kapsam yalnız `PaymentType=Reservation`) + `OrderReservationManager` (idempotent; serbest bırakılmış rezervasyon senkron döngüsünde DİRİLTİLMEZ). Karşı taraf `SalesChannelBase.SubAccountId` — alan zaten vardı. **Serbest bırakma = fiş satırının soft-delete'i** (sayaçtan düşer, denetim izi kalır). Eşleşmeyen/reçetesiz sipariş SESSİZ ATLANMAZ → `Blocked` + gerekçe. Mekanik ağ: `OrderReservationConventionTests` (**`Expire*`/`*Timeout*`/`AutoRelease*` üye adı YASAK** — refleksiyon; iki eksenin karışmaması; `PriceDifference` nullable kalması) · `OrderReservationManagerTests` · **`OrderReservationDoubleCountTests`** (50 giriş → 20 rezerve → 20 fiziki çıkış + serbest bırakma → `Available=30`, **10 değil**; serbest bırakmayı unutmak çift sayım üretir ve o da ayrıca pinli).
- **İPTAL/İADE: MEKANİZMA ≠ POLİTİKA** (2026-08-05 Hakan kararı): kıymetli maden **yasal olarak** iptal/iadeye kapalı (fiyatı piyasa koşullarına bağlı ürün); genel ürünlerde (ütü, fön…) normal kurallar geçerli. **AMA altyapı altın ürünleri için de iptal/iade DESTEKLER** — sistem yalnız UYARIR, ENGELLEMEZ; reddi kullanıcı verir ("reddetme zevkini bana bırak"). *Gerekçe: pazaryeri müşteri lehine karar verebilir ya da jest gerekebilir; kod kilitlerse yol kalmaz.* Sınır kuralı: **HİÇBİR İPTAL OTOMATİK DEĞİLDİR** — kargolanmamış sipariş bile kullanıcı onayından/reddinden geçer. Kanaldan gelen iptal talebi rezervasyonu KENDİLİĞİNDEN serbest BIRAKMAZ; "karar bekleyen iş" olarak durur ve rezervasyon tutulmaya devam eder. *Gerekçe: fiş kesilmemiş olsa bile mal fiziksel olarak hazırlanmış/kesilmiş/eritilmiş olabilir — bunu yalnız kullanıcı bilir.* Çıkış yapıldıktan sonra iptal → reddedilir (artık iade sürecidir) · iade mal fiziksel olarak kasaya GİRENE kadar stokta yok sayılır. Dönüşü olmayan nokta = **rezerve → fiziki çıkış** geçişi; ama ondan ÖNCESİ de otomatik değildir.
- **TRENDYOL KOMİSYONU KATEGORİDEN KALITIMLA GELİR** (2026-08-06 Hakan kararı: *"Kategori ağacının en belirgin parentlerine bu genel oranları geç. Child kategoriler bu parentlerden inherit yararlansın. Sonrasında satış kanalına özel komisyonları belirleriz."*): `TrendyolCategory.CommissionRate` (`decimal?`) + `TrendyolCommissionResolver` yaprak→üst→kök yürür, **en yakın DOLU ata kazanır**; hiçbir seviyede yoksa `TrendyolCommissionDefaults.PlaceholderRate` (%18,5). `null` = "bu seviyede tanımlı değil, üstten miras al" — **sıfır komisyon DEĞİL**; `0` ise meşru bir beyandır (komisyonsuz anlaşma). **Neden gerekliydi:** N11 komisyonu kategori ucundan döndürüyor, Trendyol DÖNDÜRMÜYOR (`id/name/parentId/isLeaf` yalnız) → `resolvedCommissionRate` sabit `null` geçiliyordu ve komisyon HİÇBİR reçeteye girmiyordu; hatasız, logsuz, yalnız ~%20 ucuz fiyat. **Seed 16 KÖKTEDİR** (`TrendyolCommissionSeedRates`, ExternalId anahtarlı — ad değil): 3351 yaprağın tamamı bu 16'dan miras alıyor, sıfır boşluk. Oranlar Trendyol API'sinden DEĞİL, iki yayınlanmış tablodan (ideasoft 06.02.2026 · Paraşüt) türetilmiş ORTALAMALARDIR — kaynaklar aralık veriyor ve çelişiyor; tek otorite Satıcı Paneli → *Anlaşma Bilgileri*. Seeder **yalnız BOŞ oranı doldurur**, dolu oranı asla ezmez (aksi halde her sync gerçek sözleşme oranını tahmine geri çevirirdi). ⚠ Plan **ÜRÜN BAŞINA** kurulur, import başına değil (kategori ürün-başı bir alandır).
  - **⛔ KOMİSYON KONUSU KAPANDI — YENİDEN AÇMA** (2026-08-08 Hakan kararı, "BU KONUYU KAPATALIM"). Üç madde:
    ① **Oranın tabanı = KDV DAHİL.** Eski "⚠ taban modellenmedi" açık maddesi bununla KAPANDI. Kod değişikliği
    gerekmedi: `GrossUp` zaten `taban ÷ (1 − oran/100)` ile türetilmiş satış fiyatına uygulanıyor, yani beyan
    edilen taban bugünkü hesapla tutarlı. ② **Mevcut oranlar SABİT komisyon kabul edilir.** Trendyol komisyonu
    zaten satıcıya göre değişiyor (kadın girişimci ayrı, satış hacmine göre kademeli) — yani "tek gerçek oran"
    diye bir şey yok; gerçek hesap gelene kadar bugünkü değerler sabit sayılır. ③ **Satıcı Paneli'ne erişim
    İSTENMEZ.** Hesap arkadaşınındır ve Hakan onu yalnız TEST için devraldı; ABD'den bir kez panele girildiğinde
    güvenlik uyarısı tetiklendi, karşı taraf teknik değil. Panele erişim istemek mevcut API bilgisini de
    kaybettirebilir. Gerçek hesap geldiğinde paneli Hakan kendisi açıp gereken bilgiyi verecek.
    *(Bu soru oturum içinde iki kez soruldu; kaydın amacı üçüncüyü önlemek.)* Mekanik ağ: `TrendyolCommissionResolverTests` (kalıtım · en yakın ata · yer tutucuya düşme · döngü) · `TrendyolCommissionSeedRateTests` (oran aralığı · `null`≠`0`).
- **Zaman: kayıt=UTC, görüntü=kullanıcı yerel saati** (2026-07-03 ürün kararı). Zaman damgaları UTC saklanır (`AbpClockOptions.Kind=Utc` hedefi); UI her kullanıcının tarayıcı/masaüstü saatine çevirip gösterir (merkezi dönüşüm — sayfa-başı elle çeviri YOK). İSTİSNA: kullanıcının seçtiği İŞ TARİHİ alanları (VoucherDate gibi) date-only semantiktir, timezone kaydırmasına GİRMEZ (gün kayması yasak). Geçiş planı: zaman/kültür denetim raporu.
- **ViewModel emekli:** flat edit formları GetDto-direct (`CrudEditComponentBase<TGetDto>`, Save'de `ObjectMapper.Map` Mapperly); drill/tree → Contracts input-DTO + DrillList. Client-side ViewModel YOK.
- **TEK fiziksel DB** (2026-07-10 keşfi): `AbpTenantConnectionStrings` boş — 14 tenant'ın tümü tek `TradeXpress` DB'sini paylaşıyor; migration'lar TEK DB'ye uygulanır ("3 DB'ye uygula" adımı YOK). `Integration.TradeXpress_company1/_ekuyumcu/_Service` DB'leri ESKİ projelerin kalıntısı, bu app'in değil (DOKUNMA). DbMigrator secrets için proje klasöründen çalıştırılır (repo kökünden content-root bulunamaz).
- **TEK görsel sistemi = DAM (K2 TAMAMLANDI, 2026-07-31):** `ProductImage`/`MetalImage`/`ISingleImageEditModel`/`ProductImageSourceType` + blob konteynerleri + upload servisleri TAMAMEN kaldırıldı (`RetireLegacyImageColumns` migration'ı kolonları düşürdü; format sonrası DB boş olduğundan veri göçü GEREKMEDİ). Tüm görsel/medya `Media`+`EntityMediaLink`'te: kayıt geneli `"Product"` bağlamı, varyanta özel `"{Entity}Variant"` bağlamı (`MediaEntityNames` — Good/Jewelry/Metal'in yerleşik deseni; link üzerinde varyant kolonu YOK). Push + önizleme + sipariş snapshot'ı + pazaryeri import'u tek kaynaktan okur/yazar (`MarketplacePushImageResolver` varyant→kayıt-geneli fallback'li; `MarketplaceImageDownloader` DAM'a import eder, indirilemeyen görsel ATLANIR — URL-kaynağı taşınmaz).
  - **HER MEDYA TİPİ İKİ BAĞLAMI DA TAŞIR** (2026-08-06 Hakan kuralı): medya kullanan bir kayıt tipi hem *kayıt geneli* (`{Entity}`) hem *varyant farkı* (`{Entity}Variant`) bağlamını taşır ve **üç katmanı da bağlar**: DTO alanı · form paneli · save'de `ReplaceForAsync`. Biri eksikse o tip YARIMDIR. **Neden kural:** Product'a kayıt-geneli medya bağlanmış, Good'a HİÇ bağlanmamıştı — canlıda 185 medya bağının tamamı `Product` bağlamında, `ProductVariant`'ta sıfır, Good'da ise ne DTO alanı ne panel vardı. Sapma kimsenin fark etmediği bir yerde büyüdü: "ürünün aynası" olması gereken mamül formu görselsiz açılıyordu. Yerleşim kuralı: **genel görsel kayıt seviyesinde, farklılık görselleri varyantta**; push zinciri zaten varyant→kayıt fallback'li okuyor (`MarketplacePushImageResolver`), yani iki bağlam da meşrudur ve biri diğerinin yerine geçmez. **Mekanik ağ (2026-08-07'de YAZILDI):** `MediaEntityNames.Registered` çift listesi + `MediaContextPairingTests` — her kayıtlı tipin iki bağlamı dolu/farklı, paylaşılan graf iki bağlamı da gidiş-dönüş taşıyor, silme kayıt-geneli bağı da temizliyor. (Aynı gün bulgusu: Jewelry/Metal/Stone'da kayıt-geneli medya HİÇ yazılmıyordu — graf yalnız varyant bağlamını biliyordu; üç katman da tamamlandı.)
  - **GÖRSEL PUSH'U GEÇİCİ LİNK ÜZERİNDEN GİDER — KALICI KAMUYA AÇIK ADRES GEREKMEZ** (Hakan talimatı, 2026-08-08'de tekrarlandı): akış → görsel **geçici link veren bir siteye** yüklenir → o link satış kanalına verilir → **kanal görseli KENDİ sistemine kaydeder** ve ürün bilgileriyle birlikte **kendi görsel linkini geri döndürür** → biz kaydımızı o linke göre güncelleriz. *Yani pazaryeri görseli bizden sürekli çekmez; bir kez alır ve kendi CDN'ine taşır.* **Sonuç:** "kamuya açık kalıcı adres / domain / Funnel" bir ÖN KOŞUL DEĞİLDİR ve görsel push'u bu yüzden bloke etmez. **Akış 2026-08-14'te KURULDU (Trendyol):** `TemporaryMediaLinkPublisher` (`TemporaryMediaLink:{Enabled,Endpoint,Lifetime}` — varsayılan litterbox 24h; kod varsayılanı Enabled=FALSE ki test/mock ortamı dış ağa çıkmasın, canlı host appsettings'te açık) + `MediaContentReader` (blob→bayt; MediaAppService'in "bayt döndürmem" sözleşmesi bozulmadan) → gerçek push görselleri geçici linkten gider. **Dönüş ayağı da kuruldu:** import kanalın kendi CDN adreslerini `SalesChannelTrTrendyolProduct.RemoteImageUrls`'e yazar (kolonlu — `TrendyolRemoteImageUrlsAndPushContent` migration'ı); push, görsel seti son başarılı tam push'un delil kaydındakiyle (`PushHistory.Images` id listesi) birebir ise geçici link YERİNE bu adresleri gönderir — kanala aynı görseli yeniden yutturmaz. 24h ömür, Trendyol'un asenkron çekimi için; 1 saatlik barındırıcı bilinçle seçilmedi. ⚠ litterbox türü anonim barındırıcılar AV/ağ filtrelerinde kötü ünlü (2026-08-14'te Malwarebytes, adresi taşıyan bir KOMUTU bile "Trojan.Downloader" sanıp engelledi) — canlıda engellenirse uç nokta yapılandırmadan değiştirilir. *(Bu talimat daha önce de verilmişti; 2026-08-08'de "domain planın var mı?" diye tekrar sorulduğu için buraya sabitlendi.)*
  - **Medya içeriği DEĞİŞMEZ** (2026-08-05 doğrulaması + Hakan kararı): her yükleme YENİ GUID'li blob + YENİ `Media` kaydı üretir; içerik blob'u asla üzerine yazılmaz (tek `overrideExisting` poster/küçük-resim yeniden yakalamasındadır). Bu yüzden sipariş kaleminin dondurduğu `ProductSnapshotImageUrl` gerçekten donmuştur — ürünün fotoğrafı değişse bile eski sipariş eski görseli gösterir. **Kural:** görsel değişimi üzerine yazmaz, linki başka bir `Media` kaydına çevirir; **aynı içerik zaten varsa (`ContentHash` eşleşiyorsa) yeni kayıt AÇILMAZ, mevcut kullanılır.** *Neden önemli: N11 ürünün her versiyonunu fotoğrafıyla DELİL olarak saklıyor ("23/07'de şu varyantı şu fiyata satmıştın") — aynı üründe fotoğraf değiştiği için iki farklı siparişte farklı resim görüldüğü Hakan tarafından yaşandı.* **BU AÇIK KAPANDI** (N11 2026-08-06 · Trendyol 2026-08-08; not 2026-08-10'da güncellendi — eskiden "push geçmişi yok" yazıyordu ve bu bilgi bayatlamıştı). `LastSent*` hâlâ üzerine yazılıyor (dirty-check onun işi), ama yanına **append-only delil defteri** kuruldu: `SalesChannelTrN11ProductPushHistory` + `SalesChannelTrTrendyolProductPushHistory`. SKU başına her gönderimde şunu yazar: an (UTC) · **tür** (`FullPush` / `PriceStockSync` — senkron içerik göndermediği için başlık/görsel `null` bırakılır, gönderilmeyeni yazmak yalan olurdu) · fiyat + para birimi · adet · başlık · varyant seçenekleri · görsellerin `MediaId:ContentHash` çiftleri (SIRALI — ilk görsel vitrin) · karşı taraf referansı (N11 task / Trendyol batch id). **Append-only YAPISALDIR:** entity'de hiçbir `Set*` yok, alanlar `protected set` — *değiştirilebilir bir delil delil değildir*. **BAŞARISIZ GÖNDERİM DE YAZILIR** (2026-08-10 Hakan kararı; başlangıçta yazılmıyordu): her satır `Outcome` (`Succeeded`/`Failed` — nötr `ChannelPushOutcome`, **ctor'da ZORUNLU** ki unutulup başarısız satır başarılı görünmesin) + `ErrorMessage` (yalnız `Failed`'da dolu; **kanalın kendi cümlesi** — Trendyol batch `FailureReasons`, N11 `FriendlyError`) taşır. *Gerekçe: defter yalnız başarıyı yazarken "denendi, kanal reddetti" ile "hiç denenmedi" ayırt edilemiyordu; otonom fiyat/stok güncellemesinde bir fiyatın kanala yansımama sebebini yalnız bu satır söyleyebilir.* **Eski kuralın koruduğu şey KORUNUR:** `LastSent*` terfisine DOKUNULMADI — kıyas tabanını yalnız başarı ilerletir, aksi hâlde bir sonraki tur "değişiklik yok" deyip hiç ulaşmamış fiyatı sessizce atlardı. **Yalnız SONUÇ anı yazılır** (2026-08-10 Hakan kararı: "sonuç anı yeterli") — submit anında ayrı satır YOK; bunun bilinen bedeli, Trendyol batch durumunu sorgulayan işçi hiç koşmazsa o denemenin deftere girmemesidir. **Geriye dönük çalışmaz:** kayıt özelliğin indiği tarihten SONRAKİ ilk push'la başlar. Mekanik ağ: `Push_history_is_appended_and_never_overwritten` (iki fiyat yan yana durur) · `A_completed_batch_writes_push_history` · `A_failed_batch_does_not_promote_last_sent` · `A_failed_batch_records_the_attempt_with_the_channel_reason` (sabotajla doğrulandı) · `A_rejected_sync_records_a_failed_attempt_with_its_reason`. **SAKLAMA: SONSUZ — eskime tarihi YOKTUR** (2026-08-10 Hakan kararı: *"her değişiklik saklanmalı. Eskime tarihi diye bir şey yok"*). Satır silinmez, arşivlenmez, budanmaz; **temizlik/purge/retention işi EKLENMEZ** — büyümüş bir tabloyu "temizlemek" ileride makul görünecektir ve o an bu kural hatırlanmalıdır: defterin değeri tam olarak eskimemesindedir. *Kabul edilen bedel:* başarısızlık `LastSent*`'i terfi ettirmediği için kayıt kirli kalır ve her repricing turunda (15 dk → günde 96) yeniden denenir; takılı kalan bir ürün SKU başına günde ~96 satır yazar. Cevap defteri budamak DEĞİL, takılı ürünü düzeltmektir — defter zaten onu görünür kılar. *Okuma güvende:* iki tabloda da `(TenantId, KanalÜrünId, SKU, PushedAtUtc)` indeksi var, yani büyüme geçmiş ekranını yavaşlatmaz. **KALAN AÇIKLAR:** ① **Etsy'de defter YOK** *(2026-08-21 Hakan: Etsy push hattı BEKLETİLDİ — import-only devam; push açıldığı gün İLK İŞ delil defteri + guard portu)* (entity/tablo/kayıt yolu hiç yok — Etsy gönderimleri iz bırakmıyor; birleşik okuma boş liste döner) ② geçmiş ekranı canlı veriyle HİÇ görülmedi (defterler boş, ilk gerçek gönderime kadar öyle kalacak).
- **"KAYIT-ÖNCESİ DURUM" TEK UoW'DA ENTITY REFERANSIYLA TUTULMAZ** (2026-08-14 bağımsız denetim bulgusu — sabotajla kanıtlı): EF kimlik haritası aynı satırı aynı instance'la döndürür; yerinde güncelleme (`GetAsync` → mutate → `UpdateAsync`) o instance'ı değiştirir ve "eski" diye saklanan liste **yeni değerleri gösterir**. Kayıt-öncesi/sonrası kıyası yapan her kod (devir kararı, dirty-check, denetim izi) eski tarafı **değer-tipi fotoğraf** olarak almalıdır (`ChannelRecipeInheritance.SnapshotOf` deseni — record struct imzalar). `ChannelRecipeRefresher`'ın ilk sürümü bu yüzden üretimde ÖLÜYDÜ ve testleri yeşildi (testler repository çağrılarını ayrı UoW'larda koşturuyordu). **Ders:** kayıt-öncesi/sonrası kıyası test edilirken çağrı **tek `WithUnitOfWorkAsync` sarmalında** koşulmalı (`ChannelRecipeRefreshTests.Refresh_survives_in_place_update_inside_a_single_unit_of_work`).
- **AYNI UoW İÇİNDE GERİ-OKUMA, FLUSH EDİLMEMİŞ YAZIMI GÖRMEZ** (2026-08-20, köprü testinde yakalandı — kardeş kural bir üstteki "kayıt-öncesi durum" maddesidir): `autoSave:false` ile yazılan satır yalnız EF değişiklik izleyicisindedir ve **sorgu öncesi kendiliğinden flush EDİLMEZ** → aynı UoW'daki `GetAsync` onu BULAMAZ. Bedeli sessizdi: `Create/UpdateAsync` sonunda `GetAsync` ile DTO döndüren her app service **SON yazılan seti eksik** döndürüyordu — emtia kaydında son varyantın görselleri boş geliyor, kullanıcı görseli kaybolmuş sanıp ancak sayfayı yenileyince geri geldiğini görüyordu. Önceki varyantların kurtulması TESADÜFTÜ: araya giren başka bir `autoSave:true` çağrısı onları flush ediyordu, sonuncuyu edecek kimse yoktu. **Kural:** "seti replace et" sözleşmesi taşıyan servis (`EntityMedia`/`EntityDocument`/`EntityNote.ReplaceForAsync`) yazdığını KENDİ flush eder (silme önce, ekleme sonra) — çağıranın "acaba flush oldu mu" diye bilmesi gereken bir sözleşme, üç yıl sonra da yanlış kullanılırdı. Mekanik ağ: `ProductCommodityProjectionTests.Seeded_variant_customizations_and_media_survive_the_first_save_of_the_commodity` (iddia KAYITTAN DÖNEN DTO üzerinde — ayrı UoW'da okusaydı hata görünmezdi).
- **"GÖNDERDİM" KAYDI, GÖVDEYE FİİLEN GİREN SETTEN YAZILIR — adayları yeniden çözerek DEĞİL** (2026-08-14): delil defteri/`PendingSent*` gibi "ne gönderdim" bilgisi, gönderim gövdesini kuran metodun **döndürdüğü** kümeden alınır (`TrendyolProductData.SentMediaIds`); aday listesini ikinci kez çözmek, yolda düşen kalemi (geçici link alamayan görsel) "gönderildi" diye yazar ve "göndermediğini yazma" yasağını sessizce ihlal eder. Aynı sınıf: yeniden-kullanım kapıları (kanal CDN adresini tekrar gönderme) DEFTERLE değil, adresin **hangi yerel sete ait olduğunu söyleyen damgayla** kurulur (`RemoteImageMediaIds`) — defter "son gönderilen"i bilir, adresin bayatlayıp bayatlamadığını bilmez.
- **ETSY VARYANT GÖRSELİ: TEK ÖZELLİK KURALI VE BİRLEŞTİRİLMİŞ VARYASYON** (2026-08-20; Hakan'ın verdiği resmî v3 spec'i + web doğrulaması). Etsy görseli kombinasyona DEĞİL, tek bir varyasyon **özellik değerine** bağlar: `GET/POST /v3/application/shops/{shopId}/listings/{listingId}/variation-images`, gövde `variation_images[] = {property_id, value_id, image_id}`. **Kısıtlar:** dizide yalnız TEK distinct `property_id` olabilir · en fazla 20 seçeneğe fotoğraf · listeleme başına 10 fotoğraf · POST mevcut eşlemelerin TAMAMINI ezer · yazma `listings_w` ister. **Sonuçları:** ① **İçe aktarımda** bir `value_id`'ye bağlı fotoğraf, o değeri taşıyan TÜM offering'lere (dolayısıyla birden çok ERP varyantına) aittir — "Renk=Kırmızı" fotoğrafı kırmızının tüm bedenlerine iner; bu kayıp değil, Etsy'nin modelidir. ② **Gönderimde (Etsy push açıldığında)** kombinasyona özel görsel isteniyorsa eksenler TEK bir özel özellikte BİRLEŞTİRİLİR (Hakan'ın tarifi: "Renk ve Kalıp" adlı tek özellik + "Kırmızı - Dar Kalıp" gibi birleşik değerler) — o zaman her `value_id` tam bir kombinasyona, yani TEK ERP varyantına karşılık gelir ve fotoğraf birebir bağlanır. İki ayrı özellik (`Renk` + `Kalıp`) gönderilirse Etsy fotoğraf bağlamaya İZİN VERMEZ. Birleştirme kararı push tasarımında alınır; içe aktarım her iki kurulumu da aynı `value_id → offering → varyant` zinciriyle çözer (ek kod gerekmez).
- **⭐ ÜRÜN MÜŞTERİYE BAKAR, EMTİA TEKNİĞE BAKAR** (Hakan, 2026-08-20: *"Ben bugüne kadar hiç bir Product içinde milyem tanımlandığına şahit olmadım. Bu müşterinin ilgi alanının dışında teknik bir bilgidir. Sistem ve kullanıcı tarafından bilinir. Ürüne maliyet fiyatı hesaplamada kullanılır."*). Ayrım: **`Product` = müşteriye/pazaryerine dönük yüz** (ad, görsel, nitelik, fiyat, kargo, kategori); **emtia (Metal/Scrap/Future/Jewelry/Stone/Good) = teknik gerçeklik** (milyem/faktör, takip birimi, giriş fiyatı, stok). Ürün teknik alanı TANIMLAMAZ, yalnız **TÜKETİR**: milyem ürüne değil, ürünün reçete SATIRINA emtiadan snapshot olarak düşer (`ProductVariantRecipeLine.Factor`; reçete panelinde düzenlenebilir çünkü o satır *bu üründe* o emtianın nasıl kullanıldığını anlatır) ve yalnız maliyet hesabına girer. **Sonuç kuralı: ürün formuna teknik alan EKLENMEZ; teknik alan emtiada yaşar, ürüne reçete üzerinden gelir.** **Doküman/not da ürüne AÇILMAZ** (2026-08-21 Hakan: "Gerek yok" — üründe görsel+açıklama yeter; dört emtia ailesindeki doküman/not sekmesi teknik tarafın işidir, köprünün bu alanları taşımaması eksiklik DEĞİLDİR). Bu, "hangi alan Product'a hangisi emtiaya" sorusunun standart cevabıdır — köprü tasarımında (`Product → Emtia` tohumu) da geçerlidir: ürün, doğurduğu emtianın teknik alanlarını DOLDURAMAZ, onları kullanıcı emtia formunda verir.
  - **ÖZEL KOD ALANLARI ÜRÜNDEN TÜRETİLMEZ** (Hakan, 2026-08-20: *"Good içerisinde yer alan SpecialCode alanları ise sadece kullanıcının doğru gruplama yapmasına yardımcı olan (renk, ölçü, kategori, özellik gibi) ve yine ürünle ilgili direkt organik bağı olmaması gereken teferruatlardır."*). `Good.Brand · Model · Kind · Type · Color · Size · Category · GroupCode` (ve diğer ailelerdeki karşılıkları; `SpecialCode` kataloğuna dayalı, ERPPRO'nun StokCinsi/StokTuru mirası) kullanıcının KENDİ gruplama/filtreleme düzenidir — ürünün kategorisi, niteliği ya da varyant ekseniyle EŞLEŞTİRİLMEZ, köprüden taşınmaz, "eksik doldurulmuş" sayılmaz. Ürünün müşteriye dönük "Renk" niteliği ile emtianın `Color` özel kodu AYRI şeylerdir: biri pazaryerine giden varyant ekseni, diğeri kullanıcının stok listesini süzme aracı. Otomatik eşleştirme kurmak ikisini birbirine bağlar ve kullanıcının düzenini ürüne tabi kılar.
- **⭐ ANA KÖPRÜ = `Product`** (Hakan, 2026-08-20: *"Ana köprümüz Product olacak. SaleChannelProduct varyasyonları ve Emtia varyasyonları bu yolla elde edilecek."*). Yani zincir **Emtia ⇄ Product ⇄ KanalÜrünü** biçiminde ve **Product ORTADAN kalkmaz**: emtia ile kanal ürünü arasında DOĞRUDAN yol açılmaz (ör. "Good'dan doğrudan Trendyol ürünü"), her çevrim Product'tan geçer. **Sonuçları:** ① `Product → {Metal · Scrap · Future · Jewelry · Stone · Good · Service}` çevrimi YEDİ ailenin HEPSİNDE KURULDU (2026-08-20/21: `ProductToCommodityProjector` + `ProjectTo*Async`/`ProjectDraftTo*Async` — kayıtlı VE kayıtsız üründen zengin seed; ters yön `CommodityToProductProjector`); ② `Product → SalesChannelProduct` üç kanal için var ve öyle kalır; ③ emtia listelerinden "Ürün Oluştur" ZORUNLU DEĞİLDİR (bugün yalnız `Good`'da var — kolaylık olarak kalabilir, eksik sayılmaz); ④ pazaryeri içe aktarımı da Product üretir (kanal→Product yönü), yani hub kuralı iki yönde de geçerlidir. **Bu yüzden `ProjectToGoodAsync`'in eksik alanları bir "detay" değil, köprünün kendisindeki boşluktur** — aynı olgunluk yedi ailede de gerekir.
- **KDV: TÜRKİYE BACAĞINDA HER FİYAT KDV DAHİL GİRİLİR VE ÇIKAR — ama alan AÇIK KALIR** (Hakan, 2026-08-20: *"Ürünün KDV'si gerçekten çok önemsiz bir detay benim için. Zaten her fiyatı KDV dahil olacak şekilde girişini ve çıkışını yapacağız Türkiye bacağı için. Fakat sadece Türkiye'de satış yapmayı düşünmediğimden dolayı da ucunu açık bırakıyorum."*). Sonuçları: ① satışa hazırlık panelinde KDV eksikliği **Info**'dur, hiçbir başlığı renklendirmez ve doğrulamayı engellemez (2026-08-19 kararı bu hedefin doğal sonucu) ② komisyon oranının tabanı KDV DAHİL fiyattır (yukarıdaki komisyon maddesiyle tutarlı) ③ `VatRate` alanı SİLİNMEZ ve zorunlu YAPILMAZ — yurt dışı satış açıldığında KDV/vergi bacağı yeniden konuşulacak, alan o gün anlam kazanacak. **KDV'yi engel hâline getiren bir kural EKLEME.**
- **⭐ HEDEF: FİYAT ÇOĞUNLUKLA OTOMATİK BELİRLENİP ÜRÜNE YANSIYACAK** (Hakan, 2026-08-20'de yeniden hatırlattı: *"Ürünlerin fiyatları çoğunlukla otomatik olarak belirlenip ürüne yansıtılacaktır. Biz henüz bu aşamaya gelmediğimiz için hayata geçiremiyoruz."*). Bu bir KURAL değil YÖN — bugün elle fiyat girilen her yer geçicidir, kalıcı model sanılmamalıdır. **Bugün VAR olan:** reçete → `ProductRecipeCostCalculator` net maliyet → `DerivedPriceCalculator.Calculate(netCost, margin)` → kanal push fiyatı; 15 dk'lık repricing turu; `EffectivePrice` motoru (kursuz birim = `RateMissing`, uydurma kur YOK); fiyat bandı (`MinPrice`/`MaxPrice`) emniyeti. **Bugün EKSİK olan üç şey:** ① marj yalnız KANAL kaydında (`StockItem.Margin`) yaşıyor, ürün/varyant seviyesinde fiyat politikası yok; ② türetilmiş fiyat `ProductVariantDetail.SalePrice`'a HİÇ yazılmıyor — o alanı yalnız kullanıcı ve pazaryeri içe aktarımı yazıyor (`ProductAppService`, `*Import.cs`); ③ "fiyat kaynağı" beyanı yok (stokta karşılığı var: `ProductStockPolicy.Fixed/Calculated/Unlimited`). **Geldiğimizde gözden geçirilecek kararlar:** `Variant:NoSalePrice` bugün Error'dır — otomatik fiyatta boş `SalePrice` NORMAL olur, bulgu "fiyat türetilemiyor" (kur yok / reçete yok / marj yok) hâline dönmelidir. Reçete şablonunun zorunluluğu (2026-08-20) ve "ana emtia 0 adet/0 miktar yasak" kuralı da bu hedefin ön hazırlığıdır: şablon ve reçete, otomatik fiyatın GİRDİSİDİR.
- **ARKA PLAN İŞÇİSİ ŞİRKET-SAHİPLİ KAYDA DOKUNUYORSA: ŞİRKET + KİMLİK KAYIT BAŞINA KURULUR** (2026-08-19 harita bulgusu — `TrendyolBatchStatusWorker` canlıda FİİLEN HİÇ ÇALIŞMIYORDU, log'da sıfır iz): kullanıcısız bağlamda `WorkingCompanyContextProvider` `Guid.Empty` **sentinel** döner, `ICompanyScoped` filtresi o sentinel'de TÜM şirket satırlarını eler → işçinin "bekleyen" sorgusu sessizce boş döner; tek satır geçse bile `[Authorize]`'lı app service kimliksiz çağrıyı reddeder. Desen (`TrendyolBatchStatusResolver` · `N11PendingPushResolver` · `OrderSyncManager`): ① bekleyenleri `IDataFilter.Disable<ICompanyScoped>()` ile listele ve kaydın KENDİ `CompanyId`'sini al; ② `OrchestrationIdentityScope.BuildTenantAdminPrincipalAsync()` (yoksa logla + atla, sessiz geçme); ③ kayıt başına `ICurrentCompany.Change(kaydın şirketi)` + `ICurrentPrincipalAccessor.Change(admin)` **çağıranın frame'inde**, sonra app service'in kendi metodunu çağır (ikinci finalizasyon kopyası yok). **İşçi gövdesi ayrı `*Resolver` sınıfında yaşar** ki işçi bağlamı olmadan, "şirket yok + kullanıcı yok" şartıyla test edilsin (`TrendyolBatchStatusResolverTests` — sabotajla doğrulandı). Aynı gün: bayat batch artık `MarkBatchStale` ile `PROCESSING`'ten ÇIKAR (eski yol yalnız hata metni yazıyor, çifte-batch kilidi sonsuza kadar kalıyordu); N11'de **kuyruk kapısı** (`EnsureNoPendingPushAsync`: çözülmemiş task varken yeni push/senkron onun üstüne yazmaz, önce bir kez sorgular) + `N11PendingPushResolverWorker` (5 dk) kuruldu.

## 7) Referans kaynakları (ERP iş kuralı araştırması)
"Eski projeye kendiliğinden referans yapma" kuralı **eski KOD/desen/karar taşımak** içindir. **İSTİSNA — bunlar tasarlanmış canlı araştırma kaynağı, voucher/cari/maden/bilanço işinde serbestçe bakılır:**
- **ERPPROV3** (`E:\Kodlarim\ERPPROV3`) — modern ABP yeniden-yazım; pattern/yapı kaynağı.
- **ERPPRO_Source / ERPPRO_Modernized** (`E:\Kodlarim`) — orijinal decompiled C#; iş kuralı **GROUND TRUTH**. Çelişkide orijinali esas al.
- **ERPGOLD DB** — canlı SQL: `.\SQLEXPRESS` / `ERPGOLDV2` / `sa`. SADECE OKU (research); çıkanları `.claude/research/<konu>/` altına kaydet.

## 8) Governance — mekanik konvansiyon ağları (armed)
Kurallar MEKANİK zorlanır (derleme + test); KIRMIZIYSA kural çiğnenmiş, sessiz geçilemez. İstisna = allow-list/attribute + gerekçe (asla "testi gevşetme" / `#pragma`). Allow-list'ler ve tam gerekçeler İLGİLİ dosyaların içinde yaşar — burada tekrarlanmaz.
- **Derleme-zamanı** (yalnız Domain+Domain.Shared; BannedApiAnalyzers RS0030=error): `Guid.NewGuid` · ham .NET exception ctor'ları (→ BusinessException/tipli) · `Check.NotNullOrWhiteSpace` (→ StringFieldGuard) — tam liste kök `BannedSymbols.txt`. Expression-bodied member: kökte warning, `Domain*/.editorconfig` ERROR (auto-prop + lambda muaf).
- **Test-zamanı:** EntityConventionTests (ctor'da id/tenantId yok · SetActive(bool) · ToString · protected set) · AppServiceConventionTests (elle statik entity→DTO mapper YASAK) · NavigationConventionTests (aggregate'ler arası id-only) · RazorConventionTests (yeni .razor'da `@code` YASAK · ad-hoc sembol/emoji ikon YASAK · yeni tam-nitelikli `@inject` YASAK) · LocalizationParityTests (tr/en anahtar paritesi).
- Yeni kural çıkınca buraya assert/ban ekle (golden GEÇsin, ihlal KIRMIZI).

## 9) Açık işler & dokunulmazlar
- Açık işler listesi: `.claude/research/governance/ACIK-ISLER.md` (Governance Faz A/B · Bullion portu · Voucher import · EditHost boilerplate).
- **SplitView eski yığını SİLİNMEZ** (`CrudEditComponentBase`/`CrudEditShell`/`{Entity}EditPage` + `SplitCrudView`) — ileride canlandırılacak, dokunma.
- **N11 MOCK'U AÇIK VE ÖYLE KALACAK** (2026-08-10 Hakan kararı; ~3 ay, ≈2026-11-10'a kadar). `appsettings.json`'da `N11:Mock:Enabled=true` + `N11:Endpoints:BaseUrl` bu host'a çevrili. **Gönderimlerin gerçek pazaryerine ULAŞMADIĞI BİLİNİYOR** — kaza değil tercih. **HATIRLATMA/UYARI YAPMA:** "mock açık, gerçek N11'e gitmiyor" uyarısı her raporun sonuna eklenmez; Hakan bunu biliyor ve tekrarı rahatsız edici buluyor. Kapatma kararı ona ait — kendiliğinden kapatma, kapatmayı önerme. *(Kaydın amacı: bağlam sıfırlandığında uyarının yeniden başlamasını önlemek.)*

## 10) Subagent orkestrasyonu
- **Kararsızlıkta SOR (agent→main):** Subagent kritik bir kararda (mimari · iş kuralı · geri-dönüşsüz işlem) emin olamazsa KOD YAZMADAN DUR, `SendMessage` ile main'e (bana) sorsun, cevabı bekleyip ona göre hareket etsin. Tahminle ilerleyip yanlış yol açmasın. **Her görev prompt'una bu talimatı ekle.**
- **Rapor→düzeltme döngüsü (main→aynı agent):** Subagent işi bitirip çözüm/rapor sunduğunda, endişelerimi + tespit ettiğim yanlış/eksikleri AYNI agente (context sıcak) `SendMessage`/resume ile ilet; düzeltip tamamlasın. Sıfırdan yeni agent açma — bağlamı koru.

---
*Detaylı dosya-özel kurallar: `.claude/rules/`. Eski memory arşivi (tam geçmiş): `.claude/_memory_backup_2026-06-28/`.*
