<Project>
  <PropertyGroup>
    <!-- LangVersion sabit (net10 = C# 14): 'latest' gelecekte yeni SDK ile sessizce C# 15+ açardı → sürpriz davranış.
         Sabitlemek 5-10 yıl reprodüksiyon garantiler. -->
    <LangVersion>14</LangVersion>
    <!-- Deterministik restore: her proje packages.lock.json üretir → 5-10 yıl sonra aynı transitive graf. -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
    <Version>1.0.0</Version>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
    <!-- RZ10012 = "çözülemeyen bileşen → bilinmeyen HTML elementi (sessizce görünmez)". Uyarı değil HATA:
         @using eksik / yanlış namespace / var olmayan bileşen (kolon bileşenleri dahil) derlemeyi DURDURUR,
         asla sessizce sızamaz. (Razor olmayan projelerde no-op.) -->
    <WarningsAsErrors>$(WarningsAsErrors);RZ10012</WarningsAsErrors>
    <NuGetAudit>false</NuGetAudit>
	<AbpProjectType>app</AbpProjectType>
  </PropertyGroup>

  <Target Name="NoWarnOnRazorViewImportedTypeConflicts" BeforeTargets="RazorCoreCompile">
    <PropertyGroup>
      <NoWarn>$(NoWarn);0436</NoWarn>
    </PropertyGroup>
  </Target>

  <ItemGroup>
    <None Remove="**\*.abppkg" />
    <None Remove="**\*.abppkg.analyze.json" />
    <Content Remove="$(UserProfile)\.nuget\packages\*\*\contentFiles\any\*\*.abppkg*" />
  </ItemGroup>

</Project>