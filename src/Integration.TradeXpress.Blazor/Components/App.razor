@using DevExpress.Blazor
@using Microsoft.AspNetCore.Components.Web
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration
<!DOCTYPE html>
@* Tema/boyut/dil İLK BOYAMADA doğru: ayna cookie'lerinden çözülür (bkz. App.razor.cs) — login
   son kullanıcının temasında flaşsız açılır. Cookie yok/bozuk → Blazing Berry Light varsayılanı. *@
<html lang="@_lang" data-bs-theme="@_colorMode" data-erp-size="@_sizeAttr" style="--tx-theme-primary:@_primaryHex">

<head>
    <meta charset="utf-8" />
    @* interactive-widget=resizes-content: mobil klavye açılınca LAYOUT viewport'u (ve dolayısıyla dvh'yi)
       küçültür. Tarayıcı varsayılanı (resizes-visual) yalnız görsel viewport'u küçültür, dvh sabit kalır →
       yükseklikleri dvh'ye dayanan yerleşim klavyenin ARKASINDA kalıyordu: veri girerken süreç panelinin
       Kaydet/Geri çubuğu görünmez oluyordu (2026-07-26 Hakan bildirimi). Bu, veri girişi ağırlıklı bir ERP
       için doğru kip — klavye açıkken uygulama kendini klavyenin üstüne sığdırır. *@
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, interactive-widget=resizes-content" />
    <title>Integration.TradeXpress</title>
    <base href="/" />

    <!--ABP:Styles-->
    @DxResourceManager.RegisterTheme(_initialTheme)
    @DxResourceManager.RegisterScripts()
    <!--/ABP:Styles-->

    <link href="main.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />
    <link href="Integration.TradeXpress.Blazor.Client.styles.css" rel="stylesheet"/>


    @{
        var useServer = Configuration.GetValue<bool>("Blazor:UseServerMode");
        IComponentRenderMode renderMode = useServer
            ? new InteractiveServerRenderMode(prerender: false)
            : new InteractiveWebAssemblyRenderMode(prerender: false);
    }
    <HeadOutlet @rendermode="renderMode" />
</head>

<body>

    <script src="_framework/blazor.web.js"></script>
    @* ?v= cache-bust: dosya değişince ARTIR — tarayıcı eski sürümü önbellekten kullanıp yeni
       fonksiyonları (ör. erpUx.scrollFocusPanel) görmüyordu. *@
    <script src="js/login.js?v=4"></script>

    <div id="ApplicationContainer">
        <div class="int-loader-container">
            <div class="int-loader-wrapper">
                <div class="int-loader-spinner"></div>
                <div class="int-loader-circle">
                    <span class="int-loader-initials">TX</span>
                </div>
            </div>
            <div class="int-loader-text">
                <span style="font-weight:300;">Integration</span> <strong>TradeXpress</strong>
            </div>
        </div>

        <Routes @rendermode="renderMode"/>
    </div>

    <!--ABP:Scripts-->
    <script>
        // ABP'nin WASM modülü, sunucu kullanıcıyı anonim gördüğünde bu fonksiyonla
        // lokal OIDC oturumunu temizler. Gerçek implementasyon: oidc.user anahtarlarını sil.
        window.abp = {
            utils: {
                removeOidcUser: function () {
                    Object.keys(sessionStorage)
                        .filter(function (k) { return k.indexOf('oidc.user:') === 0; })
                        .forEach(function (k) { sessionStorage.removeItem(k); });
                }
            }
        };
    </script>
    <script src="_content/Microsoft.AspNetCore.Components.WebAssembly.Authentication/AuthenticationService.js"></script>
    <!--/ABP:Scripts-->

</body>
</html>
