<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="..\..\common.props" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <RootNamespace>Integration.TradeXpress</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Integration.TradeXpress.Application\Integration.TradeXpress.Application.csproj" />
    <!-- N11 sahte sunucusu — deposunun davranış sözleşmesi burada sınanır (host'ta Debug-only referans). -->
    <ProjectReference Include="..\..\src\Integration.TradeXpress.Mocks\Integration.TradeXpress.Mocks.csproj" />
    <ProjectReference Include="..\Integration.TradeXpress.Domain.Tests\Integration.TradeXpress.Domain.Tests.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
  </ItemGroup>

  <!-- N11 round-trip testi GERÇEK Kestrel açar: taban sınıf statik HttpClient kullandığından TestServer araya
       giremez, istemcinin gerçekten ağa çıkması gerekir.
       PrivateAssets="all" ŞART: bu projeyi EntityFrameworkCore.Tests referanslıyor; referans oraya akarsa
       Microsoft.AspNetCore.Identity görünür olup ScopedGrantSeederTests'te IdentityUser/IdentityRole'ü ABP
       muadilleriyle belirsiz hâle getirip derlemeyi kırıyor (2026-08-05'te bir kez yaşandı). -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" PrivateAssets="all" />
  </ItemGroup>

</Project>
